#include <bits/stdc++.h>
using namespace std;
// 摆动序列
class Solution {
   public:
    int wiggleMaxLength(vector<int>& nums) {
        if (nums.size() <= 1)
            return nums.size();
        int curDiff = 0;
        int preDiff = 0;
    }
};